# Backend PARSER_ESCALAS – deploy (Render, Railway, etc.)
python-dotenv>=1.0.0
fastapi>=0.135.0
uvicorn[standard]>=0.41.0
sqlalchemy>=2.0.0
python-multipart>=0.0.22
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
pdfplumber>=0.11.0
pillow>=12.0.0
# PostgreSQL (para deploy online; em local usa SQLite)
psycopg2-binary>=2.9.0
# Driver alternativo (melhor SSL no Windows com Render); database.py usa se estiver instalado
psycopg[binary]>=3.1.0
# Se ainda falhar SSL no Windows: DATABASE_DRIVER=pg8000 no .env (pilha SSL diferente)
pg8000>=1.31.0
